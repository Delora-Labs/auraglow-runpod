# RunPod SDK
runpod>=1.7.0

# Note: PyTorch comes from base image (2.2.0 + CUDA 12.1)
# Don't reinstall torch/torchvision - use base image versions

# Diffusers and transformers (pinned for compatibility)
diffusers==0.25.1
transformers==4.36.2
accelerate==0.25.0

# Model loading
safetensors>=0.4.0
huggingface-hub>=0.20.0

# Image processing
Pillow>=10.0.0

# LoRA downloading
requests>=2.31.0

# PEFT for LoRA support
peft==0.7.1

# Memory optimization (use version compatible with torch 2.2)
xformers==0.0.24
